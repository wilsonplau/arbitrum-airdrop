{
  "name": "arbitrum-airdrop",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn workspace @arbitrum-airdrop/web dev",
    "build": "yarn workspace @arbitrum-airdrop/web build",
    "start": "yarn workspace @arbitrum-airdrop/web start"
  }
}
